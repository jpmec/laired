<h2>New hero ability</h2>

<%= render "form.html", changeset: @changeset,
                        action: hero_ability_path(@conn, :create) %>

<%= link "Back", to: hero_ability_path(@conn, :index) %>
